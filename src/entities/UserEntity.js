const initialState = {
    email: "",
    bearer: ""
};

const loginReducer = (currentState = initialState, action) => {
    switch (action.type) {

        default: {
            return currentState;
        }

    }
};


export default loginReducer;
